This module configures snmp and xinetd for observium. It has support for agent scripts for apache, nginx and mysql.

Remember to edit the following files to match you setup.

files/snmpd.conf
	Default community is "communityname", it is recommended that you change this.
	Also default location and contact should be changed.

files/xinetd.d-observium
	Allowed IP is set to 0.0.0.0, change it before you deploy.


Notice that this module only has support for debian, pull requests with improvements are of course appreciated.